Я добавил новую архитектуру с `ApiManager` и отдельными клиентами (`movies_client.py`, `reviews_client.py`) как описано в теме API классы.

Существующие тесты оставил без изменений, чтобы гарантировать их работоспособность.  
Также написал один новый демонстрационный тест `test_demo_api_manager.py`, который показывает, как работает новая структура.
